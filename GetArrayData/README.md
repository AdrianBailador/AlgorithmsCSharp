# GetArrayData

Computes the sum, average, max, min, max even value and max odd value
of an array using LINQ.

## Complexity

- **Time:** O(n) — each LINQ operation is a single pass over the array.
- **Space:** O(1) extra.

## Run

```bash
dotnet run --project GetArrayData
```
