# GetPrime

Extracts and prints the prime digits (2, 3, 5, 7) found within a
number, in their original order.

## Complexity

- **Time:** O(d), where d is the number of digits — i.e. O(log n).
- **Space:** O(d) for the result string.

## Run

```bash
dotnet run --project GetPrime
```
