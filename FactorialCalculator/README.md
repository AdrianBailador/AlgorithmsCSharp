# FactorialCalculator

Computes the factorial of a number both iteratively and recursively.

## Complexity

| Approach  | Time | Space |
|-----------|------|-------|
| Iterative | O(n) | O(1)  |
| Recursive | O(n) | O(n) — call stack |

## Run

```bash
dotnet run --project FactorialCalculator
```
