# ReverseNumberA

Reverses the digits of a number into a string, built digit by digit
with a `StringBuilder`.

## Complexity

- **Time:** O(d), where d is the number of digits.
- **Space:** O(d) for the result string.

## Run

```bash
dotnet run --project ReverseNumberA
```
