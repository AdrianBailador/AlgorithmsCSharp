# FindIndex

Linear search for the single element with value `1` in an array of 100
elements (otherwise all zeros), placed at a random index.

## Complexity

- **Time:** O(n) — worst case scans the whole array.
- **Space:** O(1)

## Run

```bash
dotnet run --project "AlgorithmsC#"
```
