# ReverseNumberB

Reverses the digits of a number arithmetically (no string
conversion), producing another `int`.

## Complexity

- **Time:** O(d), where d is the number of digits.
- **Space:** O(1)

## Run

```bash
dotnet run --project ReverseNumberB
```
