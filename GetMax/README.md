# GetMax

Finds the largest individual digit within a number.

## Complexity

- **Time:** O(d), where d is the number of digits — i.e. O(log n).
- **Space:** O(1)

## Run

```bash
dotnet run --project GetMax
```
