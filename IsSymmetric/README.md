# IsSymmetric

Checks whether a number reads the same forwards and backwards (a
numeric palindrome) by reversing its digits and comparing.

## Complexity

- **Time:** O(d), where d is the number of digits — i.e. O(log n).
- **Space:** O(1)

## Run

```bash
dotnet run --project IsSymmetric
```
