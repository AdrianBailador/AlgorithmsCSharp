# IsPrime

Checks whether a number is prime using trial division up to its
square root, skipping multiples of 2 and 3 (the 6k±1 optimization).

## Complexity

- **Time:** O(√n)
- **Space:** O(1)

## Run

```bash
dotnet run --project IsPrime
```
