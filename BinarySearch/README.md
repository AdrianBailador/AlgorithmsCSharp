# Binary Search

Recursive search for a target value in a sorted array by repeatedly
halving the search range.

## Complexity

- **Time:** O(log n)
- **Space:** O(log n) — recursion stack (would be O(1) with an iterative version).

## Run

```bash
dotnet run --project BinarySearch
```
