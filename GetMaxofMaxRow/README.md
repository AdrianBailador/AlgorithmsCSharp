# GetMaxofMaxRow

Finds the largest value in an array, then finds the largest individual
digit within that value.

## Complexity

- **Time:** O(n + log m), where n is the array length and m is the
  largest value in it.
- **Space:** O(1)

## Run

```bash
dotnet run --project GetMaxofMaxRow
```
