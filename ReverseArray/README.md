# ReverseArray

Builds a space-separated string representation of an array with its
elements in reverse order.

## Complexity

- **Time:** O(n)
- **Space:** O(n) for the output string.

## Run

```bash
dotnet run --project ReverseArray
```
